"use client";

import {
    Title
} from '@mantine/core';

const LoadingPage = () => {
    return (
        <div>
            <Title order={1}>Loading...</Title>
        </div>
    );
};

export default LoadingPage;